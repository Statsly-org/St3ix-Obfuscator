name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build and create distribution
        run: ./gradlew dist --no-daemon

      - name: Verify output
        run: |
          test $(find build/dist -maxdepth 1 -name 'st3ix-obfuscator-v*.jar' 2>/dev/null | wc -l) -eq 1
          test -f build/dist/config.yml.example
          test -f build/dist/run.bat
